var DataDisplay = function(){
    var that = Object.create(DataDisplay.prototype);


    that.displayData = function(fresh, widget, container, overallStyles, zoom){
    };


    that.removeDisplay = function(container) {

    };

    return that;
};

