type <%= classify(clicheName) %> {
  id: ID!
}

type Create<%= classify(clicheName) %>Input {
  id: ID
}

type Query {
  <%= camelize(clicheName) %>(id: ID!): <%= classify(clicheName) %>
}

type Mutation {
  create<%= classify(clicheName) %>(input: Create<%= classify(clicheName) %>Input!): <%= classify(clicheName) %>
}
