{
  "$schema": "http://json-schema.org/schema",
  "id": "DvSchematicsGenerateAction",
  "title": "Create an action for a Déjà Vu cliché",
  "type": "object",
  "properties": {
    "actionName": {
      "type": "string"
    },
    "clicheName": {
      "type": "string"
    },
    "skipAppComponentHtml": {
      "type": "boolean",
      "default": false,
      "description": "When true, the new action will not be added to the app.component.html file"
    },
    "skipMetadataImport": {
      "type": "boolean",
      "default": false,
      "description": "When true, the new action will not be added to the metadata file"
    }
  },
  "required": [ "actionName", "clicheName" ],
  "additionalProperties": false
}
