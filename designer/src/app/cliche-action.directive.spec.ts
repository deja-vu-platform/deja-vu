import { ClicheActionDirective } from './cliche-action.directive';

describe('WidgetDirective', () => {
  it('should create an instance', () => {
    // TODO: viewContainerRef argument
    const directive = new ClicheActionDirective(null);
    expect(directive)
      .toBeTruthy();
  });
});
