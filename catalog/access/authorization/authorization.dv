// Controls a user's access to resources.

cliche Authorization {
    /** Entities **/

    // Dummy field to surpress schema errors
    data Principal {
        _: text
     }

    data Resource {
        owner: Principal,
        viewers: [Principal]
    }
}
