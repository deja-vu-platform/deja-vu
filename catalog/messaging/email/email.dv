cliche Email {
  data Content {}

  // Shows a button that when clicked it sends an email the address
  // given in `to` and the content `content`
  widget SendEmail {
    to: text,
    content: Content
  }
}
