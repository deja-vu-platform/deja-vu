{
  "name": "potluck",
  "usedConcepts": {
    "authentication": {
      "config": {
        "reinitDbOnStartup": true
      }
    },
    "authorization": {
      "config": {
        "reinitDbOnStartup": true
      }
    },
    "transfer": {
      "config": {
        "reinitDbOnStartup": true,
        "balanceType": "items"
      }
    },
    "event": {
      "config": {
        "reinitDbOnStartup": true
      }
    },
    "group": {
      "config": {
        "reinitDbOnStartup": true
      }
    },
    "party": {
      "name": "property",
      "config": {
        "reinitDbOnStartup": true,
        "schema": {
          "title": "Party",
          "type": "object",
          "properties": {
              "title": {
                "type": "string"
              },
              "location": {
                "type": "string"
              },
              "description": {
                "type": "string"
              }
          },
          "required": ["title", "location", "description"]
        }
      }
    },
    "supply": {
      "name": "property",
      "config": {
        "reinitDbOnStartup": true,
        "schema": {
          "title": "Good",
          "type": "object",
          "properties": {
              "item": {
                "type": "string"
              },
              "unit": {
                "type": "string"
              }
          },
          "required": ["item", "unit"]
        }
      }
    }
  },
  "routes": [
    { "path": "parties", "action": "show-parties" },
    { "path": "create-party", "action": "create-party" },
    { "path": "login", "action": "landing" },
    { "path": "", "action": "landing" }
  ]
}
