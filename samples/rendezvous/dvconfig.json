{
  "name": "rendezvous",
  "gateway": {
    "config": {
      "wsPort": 3000
    }
  },
  "usedCliches": {
    "authentication": {
      "config": {
        "wsPort": 3002,
        "reinitDbOnStartup": true
      }
    },
    "authorization": {
      "config": {
        "wsPort": 3003,
        "reinitDbOnStartup": true
      }
    },
    "event": {
      "config": {
        "wsPort": 3004,
        "reinitDbOnStartup": true
      }
    },
    "geolocation": {
      "config": {
        "wsPort": 3005,
        "reinitDbOnStartup": true
      }
    },
    "group": {
      "config": {
        "wsPort": 3006,
        "reinitDbOnStartup": true
      }
    },
    "eventInfo": {
      "name": "property",
      "config": {
        "wsPort": 3007,
        "reinitDbOnStartup": true,
        "schema": {
          "title": "EventInfo",
          "type": "object",
          "properties": {
              "title": {
                "type": "string"
              },
              "description": {
                "type": "string"
              },
              "roomNumber": {
                "type": "string"
              },
              "locationDescription": {
                "type": "string"
              }
          },
          "required": ["title", "description"]
        }
      }
    }
  },
  "actions": {
    "app": {
      "names": [
        {"for": "src/app/app.component.html", "use": "rendezvous-root"}
      ]
    }
  },
  "routes": [
    { "path": "home", "action": "rendezvous-home" },
    { "path": "signup", "action": "rendezvous-signup" },
    { "path": "login", "action": "rendezvous-login" },
    { "path": "my-events", "action": "rendezvous-show-my-events" },
    { "path": "create-event", "action": "rendezvous-create-event" },
    { "path": "my-groups", "action": "rendezvous-show-my-groups" }
  ]
}
