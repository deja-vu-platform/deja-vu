import { Component } from '@angular/core';

@Component({
  selector: 'dv-grid',
  templateUrl: './grid.component.html',
  styleUrls: ['./grid.component.css'],
})
export class GridComponent {
  // TODO
  // Currently unimplemented because the behavior will
  // change if the worksurface mechanism is changed
}
